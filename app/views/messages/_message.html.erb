<div class="shadow overflow-hidden border-b border-gray-200 sm:rounded-lg my-4 py-2 pl-1">
  <div class="flex flex-row">
    <div class="basis-1/12"><%= l(message.created_at, format: :short) %></div>
    <div class="basis-3/12 text-center">
      <%= turbo_frame_tag message.user do%>
        <%= render message.user %>
      <% end %>
    </div>
    <div class="basis-7/12"><%= message.body %></div>
    <div class="basis-1/12">
      <%= link_to like_message_path(message), data: { "turbo-method": :post } do %>
        <%= render partial: "messages/heart", locals: { user: current_user, message: message } %>
        <%= render partial: "messages/likes_count", locals: { message: message } %>
      <% end %>
    </div>
  </div>
</div>